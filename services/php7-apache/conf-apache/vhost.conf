<VirtualHost *:80>
  DocumentRoot /var/www/src
  ServerName m.local.magento.com

  RewriteMap lc int:tolower

  # comment line below if want to use http proto (don't forget to update main docker-compose.yml)
  SetEnvIf X-Forwarded-Proto https HTTPS=on

  <Directory /var/www/src/>
    Options Indexes FollowSymLinks MultiViews
    AllowOverride All
    Order allow,deny
    allow from all

    SetEnv MAGE_IS_DEVELOPER_MODE 1
    SetEnv MAGE_RUN_TYPE store
    SetEnvIf Host m\.local\.magento\.com MAGE_RUN_CODE=magento

    DirectoryIndex index.php
    AddDefaultCharset Off
  </Directory>

  <Directory /var/www/src/media/>
    Options All -Indexes
    <IfModule mod_php5.c>
      php_flag engine 0
    </IfModule>
    AddHandler cgi-script .php .pl .py .jsp .asp .htm .shtml .sh .cgi
    Options -ExecCGI
  </Directory>

  php_flag max_execution_time 18000
</VirtualHost>
